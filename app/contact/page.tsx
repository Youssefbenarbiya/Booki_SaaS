import { Metadata } from "next"

export const metadata: Metadata = {
  title: `Contact`,
}

export default async function ContactPage() {

  return (
    <div>
    <p>contact page</p>
    </div>
  )
}
